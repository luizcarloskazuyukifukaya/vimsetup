# Vim Setup configuration files
This is the repository to keep the vim configuration files (specially .vimrc)
unified across different environments.

## HOW TO INSTALL
- copy the .vimrc to ~/.vimrc
(NOTE) For some plugins, you need to install files separatly, so please read
the comments in the .vimrc file for details.


